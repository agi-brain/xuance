name: xuance_env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.7
  - pip>=20.1  # pip is needed as dependency
  - pip:
    - black==20.8b1
  - mpi4py
prefix: /home/docs/.conda/envs/xuance_env